Steps to import and use a package - 

in your main go file - 
1. initialize module by "go mod init <group-id>/<artifact>"
2. write an import statement in main code.
3. download packages in local cache + declare in mod file by firing a "go mod tidy"